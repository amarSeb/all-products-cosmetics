export const metadata = {
  title: "ALL PRODUCTS COSMETICS",
  description: "Premium Cosmetics Store"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
